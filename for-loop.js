function loopWhile() {

  let i = 0;
  const end =5;
  
  while(i < end ){
    // const result = i + " " + "hello";"
    const result = `${i} hello`;
    console.log(result)
    i++
  }                        
}

function madLib(name,car,color) {
  const sentence = `Hello ypour name is ${name}. You drive ${car} which is 
}